server {
    listen 80;
    server_name api.alexandre-richard.fr;

    location / {
        proxy_pass http://backend:${LOCAL_PORT};
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
